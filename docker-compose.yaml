version: '3'

services:
  elasticsearch:
    image: elasticsearch:alpine
    ports:
      - '9200:9200'
    volumes:
      - elasticsearch-data:/usr/share/elasticsearch/data

volumes:
  elasticsearch-data:
